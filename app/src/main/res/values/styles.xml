<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="roundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Chip Selector -->
    <color name="chip_selector">@color/chip_unselected</color>

    <!-- Favorite Selector -->
    <color name="favorite_selector">@color/favorite_unselected</color>
</resources>